let difficultSpelling = "Mississippi";
let myRegex = /s+/gi; // Change this line
let result = difficultSpelling.match(myRegex);

//The '+' sign matches when the preceding character in the search pattern occurs more than once consecutively ('ss') in the string
